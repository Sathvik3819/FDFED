<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Community Payments</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Outfit:wght@100..900&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="../../css/nav.css" />

    <style>
      /* Page Header */
      .page-header {
        margin-bottom: 30px;
      }

      .page-title {
        font-size: 2rem;
        font-weight: 700;
        color: var(--primary);
        position: relative;
        padding-bottom: 10px;
      }

      .page-title::after {
        content: "";
        position: absolute;
        bottom: 0;
        left: 0;
        width: 450px;
        height: 4px;
        background: linear-gradient(90deg, var(--primary), var(--secondary));
        border-radius: 4px;
      }

      /* Stats Cards */
      .stats-container {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 20px;
        margin-bottom: 30px;
      }

      .stat-card {
        background: white;
        border-radius: var(--border-radius);
        padding: 20px;
        box-shadow: var(--shadow);
        transition: var(--transition);
      }

      .stat-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
      }

      .stat-label {
        font-size: 0.9rem;
        color: var(--gray);
        margin-bottom: 10px;
      }

      .stat-value {
        font-size: 1.8rem;
        font-weight: 700;
        display: flex;
        align-items: center;
      }

      .blue {
        color: var(--primary);
      }
      .warning {
        color: var(--warning);
      }
      .danger {
        color: var(--danger);
      }

      /* Action Buttons */
      .actions {
        display: flex;
        gap: 15px;
        margin-bottom: 25px;
      }

      .btn {
        padding: 12px 24px;
        border-radius: 8px;
        font-weight: 500;
        transition: var(--transition);
        display: inline-flex;
        align-items: center;
        gap: 8px;
        cursor: pointer;
        border: none;
      }

      .btn.primary {
        background-color: var(--primary);
        color: white;
      }

      .btn.primary:hover {
        background-color: #0b5ed7;
        transform: translateY(-2px);
        box-shadow: 0 4px 10px rgba(13, 110, 253, 0.3);
      }

      /* Search and Filter */
      .search-bar {
        display: flex;
        gap: 15px;
        margin-bottom: 25px;
      }

      .search-bar input,
      .search-bar select {
        padding: 12px;
        border: 1px solid #ddd;
        border-radius: 8px;
        font-size: 15px;
        transition: var(--transition);
      }

      .search-bar input {
        flex: 1;
      }

      .search-bar input:focus,
      .search-bar select:focus {
        border-color: var(--primary);
        outline: none;
        box-shadow: 0 0 0 3px rgba(13, 110, 253, 0.2);
      }

      /* Table Styles */
      .table-container {
        overflow-x: auto;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 25px;
      }

      th,
      td {
        padding: 15px;
        text-align: left;
        border-bottom: 1px solid #eee;
      }

      th {
        background-color: #f8f9fa;
        font-weight: 600;
        color: var(--dark);
      }

      tr:hover {
        background-color: #f8f9fa;
      }

      /* Status Badges */
      .status-badge {
        padding: 6px 12px;
        border-radius: 20px;
        font-size: 0.85rem;
        font-weight: 500;
        display: inline-block;
      }

      .status-paid {
        background-color: #d1e7dd;
        color: var(--success);
      }

      .status-pending {
        background-color: #fff3cd;
        color: var(--warning);
      }

      .status-late {
        background-color: #f8d7da;
        color: var(--danger);
      }

      /* Pagination */
      .pagination {
        display: flex;
        justify-content: center;
        gap: 10px;
      }

      .pagination button {
        padding: 8px 16px;
        border: 1px solid #ddd;
        background-color: white;
        border-radius: 5px;
        cursor: pointer;
        transition: var(--transition);
      }

      .pagination button.active {
        background-color: var(--primary);
        color: white;
        border-color: var(--primary);
      }

      .pagination button:hover:not(.active) {
        background-color: #f8f9fa;
      }

      /* Modal Styles */
      .modal {
        display: none;
        position: fixed;
        z-index: 1001;
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.5);
      }

      .modal-content {
        background-color: white;
        margin: 5% auto;
        padding: 25px;
        border-radius: var(--border-radius);
        box-shadow: var(--shadow);
        width: 90%;
        max-width: 500px;
        position: relative;
      }

      .close {
    position: absolute;
    top: 10px;
    right: 15px;
    font-size: 22px;
    font-weight: bold;
    cursor: pointer;
    color: var(--text-light);
    transition: 0.3s ease-in-out;
    padding: 25px;
}

.close:hover {
    color: var(--primary-color);
}

      .form-group {
        margin-bottom: 20px;
      }

      .form-group label {
        display: block;
        margin-bottom: 8px;
        font-weight: 500;
        color: var(--dark);
      }

      .form-group input,
      .form-group select,
      .form-group textarea {
        width: 100%;
        padding: 12px;
        border: 1px solid #ddd;
        border-radius: 8px;
        font-size: 15px;
        transition: var(--transition);
      }

      .form-group input:focus,
      .form-group select:focus,
      .form-group textarea:focus {
        border-color: var(--primary);
        outline: none;
        box-shadow: 0 0 0 3px rgba(13, 110, 253, 0.2);
      }

      .form-actions {
        display: flex;
        justify-content: flex-end;
        gap: 15px;
        margin-top: 25px;
      }

      .btn.secondary {
        background-color: var(--gray);
        color: white;
      }

      .btn.secondary:hover {
        background-color: #5c636a;
      }

      /* Responsive Styles */
      @media (max-width: 992px) {
        .bodyContainer {
          grid-template-columns: 1fr;
        }

        .adCon {
          height: auto;
          position: relative;
          top: 0;
          margin-bottom: 20px;
        }

        .ad-text {
          transform: rotate(0);
          padding: 30px;
        }

        .stats-container {
          grid-template-columns: 1fr;
        }
      }

      @media (max-width: 768px) {
        nav ul {
          gap: 10px !important;
          flex-wrap: wrap;
          justify-content: center;
        }

        .actions {
          flex-direction: column;
        }

        .search-bar {
          flex-direction: column;
        }

       /* Refresh Button Styles */
.btn.refresh {
  background-color: #17a2b8;
  display: inline-flex;
  align-items: center;
  gap: 8px;
}

.btn.refresh:hover {
  background-color: #138496;
}

.btn.refresh.refreshing i {
  animation: spin 1s linear infinite;
}

@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.last-updated {
  font-size: 0.85rem;
  color: #666;
  margin-left: 10px;
}
      }

    </style>
  </head>

  <body>
    <%- include('../../partials/managerNav.ejs') %>

    <div class="bodyContainer">
      <!-- AD SECTION -->
     <div class="adCon d-flex justify-content-center align-items-center">
        <div class="advertisement" id="ad-slider">
          <% if (locals.ads && ads.filter(ad => ad.status==="Active").length > 0) { %> <% ads.filter(ad => ad.status==="Active").forEach(ad => { %>
          <img
            src="http://localhost:3000/<%= ad.imagePath.replace(/\\/g, '/') %>"
            class="ad-slide"
            alt="Ad"
            height="100px"
            width="200px"
          />
          <% }) %> <% } else { %>
          <div
            class="d-flex justify-content-center align-items-center"
            style="
              background-color: rgba(158, 207, 250, 0.539);
              border-radius: 8px;
              color: rgb(0, 80, 193);
              border: 3px dotted blue;
              height: 100%;
              width: 100%;
            "
          >
            <p class="ad-text fs-4" style="font-weight: 600">Advertisement</p>
          </div>
          <% } %>
        </div>
      </div>

      <!-- CONTENT SECTION -->
      <div class="contentCon">
        <div class="page-header">
          <div class="page-title">Community Payments</div>
        </div>

        <div class="stats-container">
          <div class="stat-card">
            <p class="stat-label">Total payments (this month)</p>
            <h2 class="stat-value blue">
              <img
                src="/imgs/rupee.png"
                alt="rupees"
                height="25px"
                width="18px"
                style="margin-right: 5px"
              />
              <span id="totalAmount">0</span>
            </h2>
          </div>
          <div class="stat-card">
            <p class="stat-label">Pending payments</p>
            <h2 class="stat-value warning">
              <img
                src="/imgs/rupee.png"
                alt="rupees"
                height="25px"
                width="18px"
                style="margin-right: 5px"
              />
              <span id="pendingAmount">0</span>
            </h2>
          </div>
          <div class="stat-card">
            <p class="stat-label">Late payments</p>
            <h2 class="stat-value danger" id="latePayments">0</h2>
          </div>
        </div>

        <div class="actions">
          <button class="btn primary" id="bulkPayment">
            Create Combined Resident Payment
          </button>
          <button class="btn primary" id="subscriptionPayment">
            Subscription Payment
          </button>
          <button class="btn primary" id="changePlanBtn" style="background-color: #28a745;">
            Change Plan
          </button>
          <div style="display: flex; gap: 15px; align-items: center;">
    <button class="btn primary" id="refreshBtn" style="background-color: #17a2b8;">
      <i class="bi bi-arrow-clockwise"></i> Refresh
    </button>
    <label style="display: flex; align-items: center; gap: 8px; margin: 0;">
      <input type="checkbox" id="autoRefreshToggle" checked>
      <span style="font-size: 14px; color: #666;">Auto-refresh (30s)</span>
    </label>
  </div>
        </div>

        <div class="search-bar">
          <input
            type="text"
            id="searchInput"
            placeholder="Search by title or status..."
          />
          <select id="timeFilter">
             <option value="all">All Time</option>
            <option value="this-month">This Month</option>
            <option value="last-month">Last Month</option>
           
          </select>
        </div>

        <div class="table-container">
          <table id="paymentsTable">
            <thead>
              <tr>
                <th>Title</th>
                <th>Resident ID</th>
                <th>Amount</th>
                <th>Payment Deadline</th>
                <th>Payment Completion Date</th>
                <th>Payment Mode</th>
                <th>Status</th>
                <th>Remarks</th>
              </tr>
            </thead>
            <tbody id="paymentsTableBody"></tbody>
          </table>
        </div>

        <div class="pagination" id="pagination">
          <!-- Pagination buttons will be added dynamically -->
        </div>
      </div>

      <div id="bulkPaymentModal" class="modal">
        <div class="modal-content">
          <span class="close">&times;</span>
          <h2>Create Combined Resident Payment</h2>
          <form id="bulkPaymentForm">
            <div class="form-group">
              <label for="bulkPaymentTitle">Title</label>
              <input
                type="text"
                id="bulkPaymentTitle"
                required
                placeholder="e.g. Monthly Maintenance"
              />
            </div>

            <div class="form-group">
              <label for="bulkPaymentAmount">Amount per Resident (₹)</label>
              <input
                type="number"
                id="bulkPaymentAmount"
                required
                min="0"
                placeholder="Payment amount"
              />
            </div>

            <div class="form-group">
              <label for="bulkPaymentRemarks">Remarks</label>
              <textarea
                id="bulkPaymentRemarks"
                placeholder="Add any additional information here"
              ></textarea>
            </div>

            <div class="form-actions">
              <button type="submit" class="btn primary">
                Create Payments for All Residents
              </button>
              <button
                type="button"
                class="btn secondary"
                id="cancelBulkPayment"
              >
                Cancel
              </button>
            </div>
          </form>
        </div>
      </div>

      <!-- Subscription Payment Modal -->
<div id="subscriptionPaymentModal" class="modal">
  <div class="modal-content" style="max-width: 600px; padding: 25px; border-radius: 12px; background: #fff; box-shadow: 0 8px 24px rgba(0,0,0,0.15);">
    
    <span class="close close-subscription" style="float: right; font-size: 1.5rem; cursor: pointer;">&times;</span>
    <h2 style="margin-bottom: 15px; font-size: 1.8rem; color: #333; text-align: center;">
      Subscription Payment
    </h2>

    <!-- Conditional status message -->
    <% if (x) { %>
      <div style="font-size:22px;background: #fff3f3; color: rgb(72, 206, 46); padding: 10px; border-radius: 8px; margin-bottom: 20px; text-align: center;">
  Welcome to UrbanEase! Please proceed with the payment to access the website.
</div>

    <% } else if (y) { %>
      <div style="background: #fff3f3; color: #a33; padding: 10px; border-radius: 8px; margin-bottom: 20px; text-align: center;">
        Your subscription has expired. Please renew to continue accessing all features.
      </div>
    <% } %>

    <form id="subscriptionPaymentForm">

  <!-- Plan Selection -->
  <div class="form-group" style="margin-bottom: 20px;">
    <label for="subscriptionPlan" 
           style="display: block; font-weight: bold; margin-bottom: 8px;">
      Select Subscription Plan
    </label>
    <select id="subscriptionPlan" required 
            style="width: 100%; padding: 10px; border-radius: 8px; border: 1px solid #ccc;">
      <option value="">Choose a plan</option>
      <% Object.keys(planPrices).forEach(planKey => { %>
        <option value="<%= planKey %>" 
                data-price="<%= planPrices[planKey] %>"
                <%= plan === planKey ? 'selected' : '' %>>
          <%= planKey.charAt(0).toUpperCase() + planKey.slice(1) %> Plan - ₹<%= planPrices[planKey] %>
        </option>
      <% }) %>
    </select>
  </div>

  <!-- Plan Details -->
  <div id="planDetails" 
       style="background: linear-gradient(135deg, #e0f7fa, #ffffff); 
              padding: 15px; 
              border-radius: 10px; 
              margin-bottom: 20px; 
              box-shadow: 0 2px 5px rgba(0,0,0,0.05);">

    <h3 id="selectedPlanName" style="margin: 0 0 5px; color: #00796b;">
      <%= plan.charAt(0).toUpperCase() + plan.slice(1) %> Plan
    </h3>
    <p style="color: #555; font-size: 0.95rem; margin-bottom: 5px;">
      Enjoy exclusive benefits and features tailored for your community.
    </p>
    <strong id="selectedPlanPrice" style="color: #333; font-size: 1.1rem;">
      Price: ₹<%= planPrices[plan] %>
    </strong>
  </div>

  <!-- Payment Method -->
  <div class="form-group" style="margin-bottom: 20px;">
    <label for="paymentMethod" 
           style="display: block; font-weight: bold; margin-bottom: 8px;">
      Payment Method
    </label>
    <select id="paymentMethod" required 
            style="width: 100%; padding: 10px; border-radius: 8px; border: 1px solid #ccc;">
      <option value="">Select payment method</option>
      <option value="credit_card">💳 Credit Card</option>
      <option value="debit_card">🏦 Debit Card</option>
      <option value="net_banking">💻 Net Banking</option>
      <option value="upi">📱 UPI</option>
      <option value="wallet">💼 Digital Wallet</option>
    </select>
  </div>

  <!-- Payment Summary -->
  <div class="payment-summary" 
       style="background: #f9f9f9; padding: 15px; border-radius: 8px; margin-bottom: 20px;">
    <h4 style="margin-bottom: 10px; color: #333;">Payment Summary</h4>
    <div style="display: flex; justify-content: space-between; font-size: 1.1rem;">
      <span>Total Amount:</span>
      <strong id="totalAmountDisplay">₹<%= planPrices[plan] %></strong>
    </div>
  </div>

  <!-- Form Actions -->
  <div class="form-actions" 
       style="display: flex; justify-content: space-between; gap: 10px;">
    <button type="submit" 
            style="flex: 1; background: #007bff; color: #fff; padding: 12px; 
                   border-radius: 8px; font-size: 1rem; border: none; cursor: pointer;">
      Process Payment
    </button>
    <button type="button" id="cancelSubscriptionPayment" 
            style="flex: 1; background: #ccc; color: #333; padding: 12px; 
                   border-radius: 8px; font-size: 1rem; border: none; cursor: pointer;">
      Cancel
    </button>
  </div>

</form>

  </div>
</div>

      <!-- Plan Change Modal -->
      <div id="planChangeModal" class="modal">
        <div class="modal-content" style="max-width: 700px; padding: 25px; border-radius: 12px; background: #fff; box-shadow: 0 8px 24px rgba(0,0,0,0.15);">
          
          <span class="close close-plan-change" style="float: right; font-size: 1.5rem; cursor: pointer;">&times;</span>
          <h2 style="margin-bottom: 15px; font-size: 1.8rem; color: #333; text-align: center;">
            Change Subscription Plan
          </h2>

          <!-- Current Plan Info -->
          <div id="currentPlanInfo" 
               style="background: #f8f9fa; padding: 15px; border-radius: 8px; margin-bottom: 20px; border-left: 4px solid #007bff;">
            <h4 style="margin: 0 0 10px; color: #333;">Current Plan</h4>
            <div id="currentPlanDetails">
              <!-- Will be populated by JavaScript -->
            </div>
          </div>

          <!-- New Plan Selection -->
          <div class="form-group" style="margin-bottom: 20px;">
            <label for="newPlanSelect" 
                   style="display: block; font-weight: bold; margin-bottom: 8px;">
              Select New Plan
            </label>
            <select id="newPlanSelect" required 
                    style="width: 100%; padding: 10px; border-radius: 8px; border: 1px solid #ccc;">
              <option value="">Choose a new plan</option>
              <% Object.keys(planPrices).forEach(planKey => { %>
                <option value="<%= planKey %>" 
                        data-price="<%= planPrices[planKey] %>"
                        <%= plan === planKey ? 'disabled' : '' %>>
                  <%= planKey.charAt(0).toUpperCase() + planKey.slice(1) %> Plan - ₹<%= planPrices[planKey] %>
                  <%= plan === planKey ? ' (Current)' : '' %>
                </option>
              <% }) %>
            </select>
          </div>

          <!-- New Plan Details -->
          <div id="newPlanDetails" 
               style="background: linear-gradient(135deg, #e8f5e8, #ffffff); 
                      padding: 15px; 
                      border-radius: 10px; 
                      margin-bottom: 20px; 
                      box-shadow: 0 2px 5px rgba(0,0,0,0.05);
                      display: none;">

            <h3 id="newPlanName" style="margin: 0 0 5px; color: #28a745;">
              <!-- Will be populated by JavaScript -->
            </h3>
            <p style="color: #555; font-size: 0.95rem; margin-bottom: 5px;">
              Enjoy enhanced benefits and features for your community.
            </p>
            <strong id="newPlanPrice" style="color: #333; font-size: 1.1rem;">
              <!-- Will be populated by JavaScript -->
            </strong>
          </div>

          <!-- Change Options -->
          <div id="changeOptions" style="display: none;">
            <h4 style="margin-bottom: 15px; color: #333;">Choose Change Option:</h4>
            
            <!-- Immediate Change Option -->
            <div style="background: #fff3cd; border: 1px solid #ffeaa7; border-radius: 8px; padding: 15px; margin-bottom: 15px;">
              <label style="display: flex; align-items: flex-start; gap: 10px; cursor: pointer;">
                <input type="radio" name="changeOption" value="immediate" id="immediateChange" style="margin-top: 3px;">
                <div>
                  <strong style="color: #856404;">Change Now (Immediate)</strong>
                  <p style="margin: 5px 0; color: #856404; font-size: 0.9rem;">
                    ⚠️ <strong>Caution:</strong> This will immediately change your plan and charge the difference. 
                    Any unused time from your current plan will be forfeited.
                  </p>
                  <div id="immediateCost" style="font-weight: bold; color: #dc3545;">
                    <!-- Will show cost difference -->
                  </div>
                </div>
              </label>
            </div>

            <!-- Next Cycle Change Option -->
            <div style="background: #d4edda; border: 1px solid #c3e6cb; border-radius: 8px; padding: 15px;">
              <label style="display: flex; align-items: flex-start; gap: 10px; cursor: pointer;">
                <input type="radio" name="changeOption" value="nextCycle" id="nextCycleChange" style="margin-top: 3px;">
                <div>
                  <strong style="color: #155724;">Change from Next Billing Cycle</strong>
                  <p style="margin: 5px 0; color: #155724; font-size: 0.9rem;">
                    ✅ <strong>Recommended:</strong> Your plan will change from the next billing cycle. 
                    You'll continue using your current plan until then.
                  </p>
                  <div id="nextCycleInfo" style="font-weight: bold; color: #28a745;">
                    <!-- Will show next cycle info -->
                  </div>
                </div>
              </label>
            </div>
          </div>

          <!-- Form Actions -->
          <div class="form-actions" 
               style="display: flex; justify-content: space-between; gap: 10px; margin-top: 25px;">
            <button type="button" id="confirmPlanChange" 
                    style="flex: 1; background: #28a745; color: #fff; padding: 12px; 
                           border-radius: 8px; font-size: 1rem; border: none; cursor: pointer; display: none;">
              Confirm Plan Change
            </button>
            <button type="button" id="cancelPlanChange" 
                    style="flex: 1; background: #ccc; color: #333; padding: 12px; 
                           border-radius: 8px; font-size: 1rem; border: none; cursor: pointer;">
              Cancel
            </button>
          </div>

        </div>
      </div>

    </div>

    <script src="../../js/communityManager/payments.js"></script>
    <script src="../../js/sidebar.js"></script>
  </body>
</html>
