
    <!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Common Space Bookings</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Outfit:wght@100..900&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="../../css/nav.css" />

    <style>
     
         
     
      .section-title {
        position: relative;
        padding-bottom: 15px;
        margin-bottom: 25px;
        border-bottom: 2px solid #e9ecef;
      }

      .section-title::after {
        content: "";
        position: absolute;
        bottom: -2px;
        left: 0;
        width: 120px;
        height: 3px;
        background: linear-gradient(90deg, var(--primary), var(--secondary));
        border-radius: 3px;
      }

      /* Stats Container */
      .stats-container {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 20px;
        margin-bottom: 30px;
      }

      .stat-card {
        background: white;
        border-radius: 5px;
        padding: 20px;
        box-shadow: var(--shadow);
        transition: var(--transition);
        text-align: center;
      }

      .stat-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.12);
      }

      .stat-card h3 {
        font-size: 1.1rem;
        color: var(--gray);
        margin-bottom: 10px;
      }

      .stat-number {
        font-size: 2.2rem;
        font-weight: 700;
        display: block;
      }

      /* Search Bar */
      .search-bar {
        display: flex;
        margin-bottom: 25px;
        gap: 10px;
      }

      .search-bar input {
        flex: 1;
        padding: 12px 15px;
        border: 1px solid #ddd;
        border-radius: var(--border-radius);
        font-size: 1rem;
        transition: var(--transition);
      }

      .search-bar input:focus {
        border-color: var(--primary);
        outline: none;
        box-shadow: 0 0 0 3px rgba(13, 110, 253, 0.2);
      }

      .search-bar button {
        padding: 12px 20px;
        background-color: var(--primary);
        color: white;
        border: none;
        border-radius: var(--border-radius);
        cursor: pointer;
        transition: var(--transition);
        font-weight: 500;
      }

      .search-bar button:hover {
        background-color: #0b5ed7;
        transform: translateY(-2px);
      }

      /* Booking Cards Container */
      .bookings-container {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 20px;
        margin-bottom: 30px;
      }

      /* Booking Card */
      .booking-card {
        background: white;
        border-radius: var(--border-radius);
        box-shadow: var(--shadow);
        padding: 20px;
        transition: var(--transition);
        border-top: 4px solid var(--primary);
        position: relative;
        overflow: hidden;
      }

      .booking-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
      }

      .booking-card-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 15px;
        padding-bottom: 10px;
        border-bottom: 1px solid var(--light-gray);
      }

      .booking-id {
        font-size: 0.85rem;
        color: var(--gray);
        font-weight: 600;
      }

      .booking-space {
        font-size: 1.2rem;
        font-weight: 600;
        color: var(--dark);
        margin-bottom: 5px;
      }

      .booking-datetime {
        display: flex;
        align-items: center;
        gap: 8px;
        color: var(--gray);
        margin-bottom: 15px;
        font-size: 0.95rem;
      }

      .booking-datetime i {
        color: var(--primary);
      }

      .booking-status {
        position: absolute;
        top: 15px;
        right: 15px;
        font-size: 0.75rem;
        font-weight: 600;
        padding: 4px 10px;
        border-radius: 20px;
      }

      .status-Pending {
        background-color: #fff3cd9f;
        color: var(--warning);
      }

      .status-Booked {
        background-color: #d1e7dd;
        color: var(--success);
      }

      .status-avalaible {
        background-color: rgba(0, 102, 255, 0.363);
        color: rgb(7, 58, 135);
      }

      .status-Rejected {
        background-color: #f8d7da;
        color: var(--danger);
      }

      .booking-actions {
        display: flex;
        gap: 10px;
        margin-top: 15px;
        flex-wrap: wrap;
      }

      .available-btn {
        padding: 8px 16px;
        background-color: var(--primary);
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        transition: var(--transition);
        font-size: 0.9rem;
        display: flex;
        align-items: center;
        gap: 5px;
      }

      .approve-btn {
        padding: 8px 16px;
        background-color: var(--success);
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        transition: var(--transition);
        font-size: 0.9rem;
        display: flex;
        align-items: center;
        gap: 5px;
      }

      .reject-btn {
        padding: 8px 16px;
        background-color: var(--danger);
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        transition: var(--transition);
        font-size: 0.9rem;
        display: flex;
        align-items: center;
        gap: 5px;
      }

      .view-btn {
        padding: 8px 16px;
        background-color: rgb(212, 201, 0);
        color: rgb(255, 255, 255);
        border: none;
        border-radius: 4px;
        cursor: pointer;
        transition: var(--transition);
        font-size: 0.9rem;
        display: flex;
        align-items: center;
        gap: 5px;
      }

      .available-btn:hover,
      .approve-btn:hover,
      .reject-btn:hover,
      .view-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      }

      /* Popup Styles */
      .popup {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        backdrop-filter: blur(2px);
        -webkit-backdrop-filter: blur(10px);
        z-index: 2000;
        justify-content: center;
        align-items: center;
      }

      .popup-content {
        background: white;
        padding: 2rem;
        border-radius: var(--border-radius);
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        width: 90%;
        max-width: 600px;
        max-height: 90vh;
        overflow-y: auto;
        position: relative;
        animation: popupFadeIn 0.3s ease-out;
      }

      @keyframes popupFadeIn {
        from {
          opacity: 0;
          transform: translateY(-20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .close-btn {
    position: absolute;
    top: 10px;
    right: 15px;
    font-size: 22px;
    font-weight: bold;
    cursor: pointer;
    color: var(--text-light);
    transition: 0.3s ease-in-out;
    padding: 25px;
}

.close-btn:hover {
    color: var(--primary-color);
}

      .form-title {
        margin: 0 0 1.5rem 0;
        color: var(--dark);
        font-size: 1.5rem;
        font-weight: 600;
        letter-spacing: -0.5px;
        text-align: center;
        position: relative;
        padding-bottom: 1rem;
        border-bottom: 2px solid var(--light-gray);
      }

      .form-title::after {
        content: "";
        position: absolute;
        bottom: -2px;
        left: 50%;
        transform: translateX(-50%);
        width: 60px;
        height: 3px;
        background: linear-gradient(90deg, var(--primary), var(--secondary));
        border-radius: 3px;
      }

      .form-group {
        margin-bottom: 1.5rem;
        display: flex;
        flex-direction: column;
      }

      .form-group label {
        margin-bottom: 0.5rem;
        font-weight: 600;
        color: var(--dark);
        font-size: 0.9rem;
      }

      .form-button {
        outline: none;
        border: none;
        padding: 5px;
        background-color: red;
        color: white;
        border-radius: 5px;
        font-weight: 600;
      }

      /* Empty State */
      .empty-state {
        text-align: center;
        padding: 40px 20px;
        background: white;
        border-radius: var(--border-radius);
        box-shadow: var(--shadow);
      }

      .empty-state i {
        font-size: 3rem;
        color: var(--light-gray);
        margin-bottom: 15px;
      }

      .empty-state h3 {
        color: var(--gray);
        margin-bottom: 10px;
      }

      .empty-state p {
        color: var(--gray);
        opacity: 0.8;
      }

      /* Responsive Styles */
      @media (max-width: 1200px) {
        .bookings-container {
          grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
        }
      }

      @media (max-width: 992px) {
        .bodyContainer {
          grid-template-columns: 1fr;
        }

        .adCon {
          height: auto;
          position: relative;
          top: 0;
        }

        .ad-text {
          transform: rotate(0);
          padding: 30px;
        }

        .stats-container {
          grid-template-columns: repeat(2, 1fr);
        }
      }

      @media (max-width: 768px) {
        nav ul {
          gap: 10px !important;
          flex-wrap: wrap;
          justify-content: center;
        }

        .stats-container {
          grid-template-columns: 1fr;
        }

        .search-bar {
          flex-direction: column;
        }

        .booking-actions {
          flex-direction: column;
        }

        .booking-actions button {
          width: 100%;
        }
      }

      @media (max-width: 576px) {
        .bookings-container {
          grid-template-columns: 1fr;
        }

        .booking-card {
          padding: 15px;
        }
      }
      /* New styles for management section */
      .management-section {
        background: white;
        border-radius: var(--border-radius);
        box-shadow: var(--shadow);
        padding: 25px;
        margin-bottom: 30px;
      }
      
      .management-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
      }
      
      .toggle-management {
        background-color: var(--primary);
        color: white;
        border: none;
        padding: 8px 15px;
        border-radius: 4px;
        cursor: pointer;
        transition: var(--transition);
      }
      
      .toggle-management:hover {
        background-color: #0b5ed7;
      }
      
      .space-list {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
        gap: 15px;
        margin-top: 20px;
      }
      
      .space-item {
        background: #f8f9fa;
        border-radius: 5px;
        padding: 15px;
        border-left: 4px solid var(--primary);
        position: relative;
      }
      
      .space-item h4 {
        margin-bottom: 10px;
        color: var(--dark);
      }
      
      .space-item p {
        margin-bottom: 5px;
        font-size: 0.9rem;
        color: var(--gray);
      }
      
      .space-actions {
        position: absolute;
        top: 10px;
        right: 10px;
        display: flex;
        gap: 5px;
      }
      
      .edit-space-btn, .delete-space-btn {
        background: none;
        border: none;
        cursor: pointer;
        font-size: 0.9rem;
        padding: 2px 5px;
        border-radius: 3px;
      }
      
      .edit-space-btn {
        color: var(--primary);
      }
      
      .delete-space-btn {
        color: var(--danger);
      }
      
      /* Form styles for adding/editing spaces */
      .space-form {
        display: none;
        margin-top: 20px;
        padding: 20px;
        background: #f8f9fa;
        border-radius: 5px;
      }
      
      .space-form.active {
        display: block;
      }
      
      .form-row {
        display: flex;
        gap: 15px;
        margin-bottom: 15px;
      }
      
      .form-group {
        flex: 1;
      }
      
      .form-actions {
        display: flex;
        justify-content: flex-end;
        gap: 10px;
        margin-top: 15px;
      }
      
      /* Rules section */
      .rules-section {
        margin-top: 30px;
      }
      
      .rules-textarea {
        width: 100%;
        min-height: 150px;
        padding: 10px;
        border: 1px solid #ddd;
        border-radius: 5px;
        font-family: inherit;
      }
      
      .save-rules-btn {
        margin-top: 15px;
        background-color: var(--success);
        color: white;
        border: none;
        padding: 8px 15px;
        border-radius: 4px;
        cursor: pointer;
      }
    </style>
  </head>

  <body>
    <%- include('../../partials/managerNav.ejs', { message: typeof message !==
    'undefined' ? message : null }) %>

    <div class="bodyContainer">
      <!-- AD SECTION -->
      <div class="adCon d-flex justify-content-center align-items-center">
        <div class="advertisement" id="ad-slider">
          <% if (locals.ads && ads.filter(ad => ad.status==="active").length > 0) { %> <% ads.filter(ad => ad.status==="active").forEach(ad => { %>
          <img
            src="http://localhost:3000/<%= ad.imagePath.replace(/\\/g, '/') %>"
            class="ad-slide"
            alt="Ad"
            height="100px"
            width="200px"
          />
          <% }) %> <% } else { %>
          <div
            class="d-flex justify-content-center align-items-center"
            style="
              background-color: rgba(158, 207, 250, 0.539);
              border-radius: 8px;
              color: rgb(0, 80, 193);
              border: 3px dotted blue;
              height: 100%;
              width: 100%;
            "
          >
            <p class="ad-text fs-4" style="font-weight: 600">Advertisement</p>
          </div>
          <% } %>
        </div>
      </div>

      <!-- CONTENT SECTION -->
      <div class="contentCon">
        <div class="section-title">
          <h1>Common Space Bookings</h1>
        </div>

        <!-- Management Section -->
        <div class="management-section">
          <div class="management-header">
            <h2>Manage Common Spaces</h2>
            <button class="toggle-management" id="toggleManagement">
              Show Management
            </button>
          </div>
          
          <div id="managementContent" style="display: none;">
            <!-- Add New Space Form -->
            <button class="toggle-management" id="addSpaceBtn">
              Add New Space
            </button>
            
            <div class="space-form" id="spaceForm">
              <h4 id="formTitle">Add New Common Space</h4>
              <form id="spaceFormElement">
                <div class="form-row">
                  <div class="form-group">
                    <label for="spaceType">Space Type</label>
                    <select class="form-control" id="spaceType" name="spaceType" required>
                      <option value="">Select Type</option>
                      <option value="Clubhouse">Clubhouse</option>
                      <option value="Banquet Hall">Banquet Hall</option>

                     
                      <option value="Swimming Pool">Swimming Pool</option>
                      <option value="Tennis Court">Tennis Court</option>
                      <option value="Badminton Court">Badminton Court</option>
                      <option value="Basketball Court">Basketball Court</option>
                      <option value="Indoor Games Room">Indoor Games Room</option>
                      <option value="Amphitheatre">Amphitheatre</option>
                      <option value="Guest Room">Guest Room</option>
                      <option value="Other">Other</option>
                    </select>
                  </div>
                  <div class="form-group">
                    <label for="spaceName">Space Name</label>
                    <input type="text" class="form-control" id="spaceName" name="spaceName" required>
                  </div>
                </div>
                
                <div class="form-row">
                  <div class="form-group">
                    <label for="bookable">Bookable</label>
                    <select class="form-control" id="bookable" name="bookable">
                      <option value="true">Yes</option>
                      <option value="false">No</option>
                    </select>
                  </div>
                  <div class="form-group">
                    <label for="maxHours">Max Booking Duration (hours)</label>
                    <input type="number" class="form-control" id="maxHours" name="maxHours" min="1" value="4">
                  </div>
                </div>

                <div class="form-group">
                  <label for="bookingRent">Rent (per hour)</label>
                  <input type="text" class="form-control" id="bookingRent" name="bookingRent" />
                </div>
                
                <div class="form-group">
                  <label for="bookingRules">Booking Rules</label>
                  <textarea class="form-control" id="bookingRules" name="bookingRules" rows="3"></textarea>
                </div>

                
                <div class="form-actions">
                  <button type="button" class="btn btn-secondary" id="cancelSpaceBtn">Cancel</button>
                  <button type="submit" class="btn btn-primary">Save Space</button>
                </div>
              </form>
            </div>
            
            <!-- Spaces List -->
         <div class="space-list" id="spacesList">
  <% if (locals.community && community.commonSpaces && community.commonSpaces.length > 0) { %>
    <% community.commonSpaces.forEach(space => { %>
      <div class="space-item" data-id="<%= space._id %>">
        <div class="space-actions">
          <button class="edit-space-btn" data-space='<%= JSON.stringify(space) %>'>
            <i class="bi bi-pencil"></i>
          </button>
          <button class="delete-space-btn" data-id="<%= space._id %>">
            <i class="bi bi-trash"></i>
          </button>
        </div>
        <h4><%= space.name %></h4>
        <p><strong>Type:</strong> <%= space.type %></p>
        <p><strong>Bookable:</strong> <%= space.bookable ? 'Yes' : 'No' %></p>
        <% if (space.bookable) { %>
          <p><strong>Max Hours:</strong> <%= space.maxBookingDurationHours || 'Not specified' %></p>
        <% } %>
        <% if (space.bookingRules) { %>
          <p><strong>Rules:</strong> <%= space.bookingRules.substring(0, 50) %>...</p>
        <% } %>
      </div>
    <% }) %>
  <% }
   else { %>
    <div class="empty-state">
      <i class="bi bi-building"></i>
      <h3>No Common Spaces Configured</h3>
      <p>Add your first common space to get started.</p>
    </div>
  <% } %>
</div>
            
            <!-- Booking Rules Section -->
            
          </div>
        </div>

        <!-- Existing Booking Stats and List -->
        <div class="stats-container">
          <div class="stat-card" style="border-left: 6px solid var(--primary)">
            <h3>Today's Bookings</h3>
            <span class="stat-number" style="color: var(--primary)">20</span>
          </div>
          <div class="stat-card" style="border-left: 6px solid var(--danger)">
            <h3>Pending Approvals</h3>
            <span class="stat-number" style="color: var(--danger)">3</span>
          </div>
          <div class="stat-card" style="border-left: 6px solid var(--success)">
            <h3>Approved</h3>
            <span class="stat-number" style="color: var(--success)">13</span>
          </div>
        </div>

        <div class="search-bar">
          <input type="text" placeholder="Search by space, date or status..." />
          <button class="approve-all">Search</button>
        </div>

        <div class="bookings-container" id="bookingsContainer">
          <% if (locals.csb && csb.length > 0) { %> <% csb.forEach(c => { %>
          <div class="booking-card">
            <div class="booking-card-header">
              <span class="booking-id">ID: <%= c._id %></span>
              <span class="booking-status status-<%= c.status %>"
                ><%= c.status %></span
              >
            </div>

            <h3 class="booking-space"><%= c.name %></h3>

            <div class="booking-datetime">
              <i class="bi bi-calendar-event"></i>
              <span><%= c.Date %>, <%= c.from %> - <%= c.to %></span>
            </div>

            <div class="booking-actions">
              <% if (c.status ==="Pending") { %>
              <button class="available-btn" data-id="<%= c._id %>">
                <i class="bi bi-eye"></i> Check Availability
              </button>
              <% } else if(c.status==="avalaible"){ %>
              <button class="approve-btn" data-id="<%= c._id %>">
                <i class="bi bi-check-circle-fill"></i> Approve
              </button>
              <button class="reject-btn" data-id="<%= c._id %>">
                <i class="bi bi-x-circle-fill"></i> Reject
              </button>
              <% } else { %>
              <button class="view-btn" data-id="<%= c._id %>">
                <i class="bi bi-eye"></i> View Details
              </button>
              <% } %>
            </div>
          </div>
          <% }) %> <% } else { %>
          <div class="empty-state">
            <i class="bi bi-calendar-x"></i>
            <h3>No Bookings Found</h3>
            <p>There are currently no common space bookings to display.</p>
          </div>
          <% } %>
        </div>
      </div>
    </div>

<div id="bookingDetailsPopup" class="popup" style="display:none;">
  <div class="popup-content">
    <h3>Booking Details</h3>
    <div id="detailsContent"></div>
    <button onclick="closeForm('details')">Close</button>
  </div>
</div>

    <div id="bookingDetailsPopup" class="popup" style="display:none;">
  <div class="popup-content">
    <h3>Booking Details</h3>
    <div id="detailsContent"></div>
    <button onclick="closeForm('details')">Close</button>
  </div>
</div>

    <script src="../../js/communityManager/CSB.js"></script>
    <script src="../../js/sidebar.js"></script>
    
    
  </body>
</html>