<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Common Space Bookings</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Outfit:wght@100..900&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="../../css/nav.css" />
    <link rel="stylesheet" href="/notyf/notyf.min.css" />

    <style>
      .section-title {
        position: relative;
        padding-bottom: 15px;
        margin-bottom: 25px;
        border-bottom: 2px solid #e9ecef;
      }

      .section-title::after {
        content: "";
        position: absolute;
        bottom: -2px;
        left: 0;
        width: 120px;
        height: 3px;
        background: linear-gradient(90deg, var(--primary), var(--secondary));
        border-radius: 3px;
      }

      .stats-container {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 20px;
        margin-bottom: 30px;
      }

      .stat-card {
        background: white;
        border-radius: 5px;
        padding: 20px;
        box-shadow: var(--shadow);
        transition: var(--transition);
        text-align: center;
      }

      .stat-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.12);
      }

      .stat-card h3 {
        font-size: 1.1rem;
        color: var(--gray);
        margin-bottom: 10px;
      }

      .stat-number {
        font-size: 2.2rem;
        font-weight: 700;
        display: block;
      }

      .search-bar {
        display: flex;
        margin-bottom: 25px;
        gap: 10px;
      }

      .search-bar input {
        flex: 1;
        padding: 12px 15px;
        border: 1px solid #ddd;
        border-radius: var(--border-radius);
        font-size: 1rem;
        transition: var(--transition);
      }

      .search-bar input:focus {
        border-color: var(--primary);
        outline: none;
        box-shadow: 0 0 0 3px rgba(13, 110, 253, 0.2);
      }

      .search-bar button {
        padding: 12px 20px;
        background-color: var(--primary);
        color: white;
        border: none;
        border-radius: var(--border-radius);
        cursor: pointer;
        transition: var(--transition);
        font-weight: 500;
      }

      .search-bar button:hover {
        background-color: #0b5ed7;
        transform: translateY(-2px);
      }

      .refresh-btn {
        padding: 12px 15px !important;
        background-color: var(--success) !important;
        margin-left: 5px;
      }

      .refresh-btn:hover {
        background-color: #198754 !important;
      }

      .refresh-btn:disabled {
        background-color: #6c757d !important;
        cursor: not-allowed;
      }

      .bookings-container {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 20px;
        margin-bottom: 30px;
      }

      .booking-card {
        background: white;
        border-radius: var(--border-radius);
        box-shadow: var(--shadow);
        padding: 20px;
        transition: var(--transition);
        border-top: 4px solid var(--primary);
        position: relative;
        overflow: hidden;
      }

      .empty-state {
        grid-column: 1/-1;
      }

      .booking-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
      }

      .booking-card-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 15px;
        padding-bottom: 10px;
        border-bottom: 1px solid var(--light-gray);
      }

      .booking-id {
        font-size: 0.85rem;
        color: var(--gray);
        font-weight: 600;
      }

      .booking-space {
        font-size: 1.2rem;
        font-weight: 600;
        color: var(--dark);
        margin-bottom: 5px;
      }

      .booking-datetime {
        display: flex;
        align-items: center;
        gap: 8px;
        color: var(--gray);
        margin-bottom: 15px;
        font-size: 0.95rem;
      }

      .booking-datetime i {
        color: var(--primary);
      }

      .booking-status {
        position: absolute;
        top: 15px;
        right: 15px;
        font-size: 0.75rem;
        font-weight: 600;
        padding: 4px 10px;
        border-radius: 20px;
      }

      .status-Pending {
        background-color: #fff3cd9f;
        color: var(--warning);
      }

      .status-Booked {
        background-color: #d1e7dd;
        color: var(--success);
      }

      .status-available {
        background-color: rgba(0, 102, 255, 0.363);
        color: rgb(7, 58, 135);
      }

      .status-Rejected {
        background-color: #f8d7da;
        color: var(--danger);
      }

      .booking-actions {
        display: flex;
        gap: 10px;
        margin-top: 15px;
        flex-wrap: wrap;
      }

      .available-btn {
        padding: 8px 16px;
        background-color: var(--primary);
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        transition: var(--transition);
        font-size: 0.9rem;
        display: flex;
        align-items: center;
        gap: 5px;
      }

      .approve-btn {
        padding: 8px 16px;
        background-color: var(--success);
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        transition: var(--transition);
        font-size: 0.9rem;
        display: flex;
        align-items: center;
        gap: 5px;
      }

      .reject-btn {
        padding: 8px 16px;
        background-color: var(--danger);
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        transition: var(--transition);
        font-size: 0.9rem;
        display: flex;
        align-items: center;
        gap: 5px;
      }

      .view-btn {
        padding: 8px 16px;
        background-color: rgb(212, 201, 0);
        color: rgb(255, 255, 255);
        border: none;
        border-radius: 4px;
        cursor: pointer;
        transition: var(--transition);
        font-size: 0.9rem;
        display: flex;
        align-items: center;
        gap: 5px;
      }

      .available-btn:hover,
      .approve-btn:hover,
      .reject-btn:hover,
      .view-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      }

      .status-badge {
        display: inline-flex;
        align-items: center;
        padding: 0.35rem 0.75rem;
        border-radius: 20px;
        font-size: 0.8rem;
        font-weight: 600;
        text-transform: capitalize;
        letter-spacing: 0.3px;
        box-shadow: inset 0 0 0 1px rgba(0, 0, 0, 0.05);
      }

      .status-badge::before {
        content: "";
        display: inline-block;
        width: 8px;
        height: 8px;
        border-radius: 50%;
        margin-right: 6px;
        background: currentColor;
        opacity: 0.7;
      }

      .status-Pending {
        background-color: rgba(234, 179, 8, 0.1);
        color: #b45309;
      }

      .status-Approved {
        background-color: rgba(16, 185, 129, 0.1);
        color: #047857;
      }

      .status-Rejected {
        background-color: rgba(239, 68, 68, 0.1);
        color: #b91c1c;
      }

      .status-Cancelled {
        background-color: rgba(107, 114, 128, 0.1);
        color: #4b5563;
      }

      .status-Completed {
        background-color: rgba(6, 212, 57, 0.95);
        color: white;
      }

      .popup {
        position: fixed;
        inset: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        background: rgba(17, 24, 39, 0.6);
        /* dark overlay */
        z-index: 1000;
      }

      /* Popup content box */
      .popup-content {
        position: relative;
        background: #fff;
        padding: 20px;
        border-radius: 12px;
        width: 90%;
        max-width: 1000px;
        max-height: 90vh;
        overflow-y: auto;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
        animation: fadeIn 0.3s ease-in-out;
      }

      .icon-col {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 30px;
        height: 30px;
        background-color: rgba(59, 130, 246, 0.1);
        border-radius: 50%;
        margin-right: 10px;
      }

      /* Header */
      .popup-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 10px 24px;
        border-bottom: 1px solid #e5e7eb;
        background: #f9fafb;
        border-radius: 12px 12px 0 0;
      }

      .popup-header h2 {
        font-size: 1.25rem;
        font-weight: 600;
        color: #0010c4;
        display: flex;
        align-items: center;
      }

      .close-btn {
        background: none;
        position: absolute;
        right: 5%;
        top: 5%;
        border: none;
        font-size: 1.5rem;
        color: #6b7280;
        cursor: pointer;
        transition: color 0.2s;
      }

      .close-btn:hover {
        color: #db0000;
      }

      /* Body */
      .popup-body {
        padding: 24px;
        background-color: rgba(232, 232, 232, 0.489);
      }

      /* Booking details grid */
      .details-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 16px 24px;
        margin-bottom: 20px;
        padding: 1rem;
        background: #ffffff;
        border-radius: 8px;
      }

      .detail-item {
        display: flex;
        align-items: center;
        padding: 5px;
      }

      .detail-label {
        font-size: 0.85rem;
        font-weight: 500;
        color: #6b7280;
      }

      .detail-value {
        font-size: 1rem;
        font-weight: 600;
        color: #111827;
        margin-top: 4px;
      }

      .detail-item.col-span-2 {
        grid-column: span 2;
      }

      /* Status badges */
      .detail-badge {
        display: inline-block;
        padding: 6px 12px;
        font-size: 0.9rem;
        font-weight: 600;
        border-radius: 20px;
      }

      /* Example status colors */
      .detail-badge.Approved {
        background: #d1fae5;
        color: #065f46;
      }

      .detail-badge.Pending {
        background: #fef3c7;
        color: #92400e;
      }

      .detail-badge.Rejected,
      .detail-badge.Cancelled {
        background: #fee2e2;
        color: #991b1b;
      }

      /* Cancellation box */
      .cancellation-box {
        padding: 16px;
        border-radius: 8px;
        border: 1px solid #fecaca;
        background: #fef2f2;
      }

      .cancellation-box h4 {
        font-size: 1rem;
        font-weight: 600;
        color: #b91c1c;
        margin-bottom: 12px;
      }

      .payment-box {
        display: grid;
        grid-template-columns: 1fr 1fr;
        padding: 16px;
        border-radius: 8px;
        border: 1px solid #d1e7dd;
        background: #d4edda83;
      }

      .payment-box h4 {
        grid-column: 1/-1;
        font-size: 1rem;
        font-weight: 600;
        color: #00d532;
        margin-bottom: 12px;
      }

      /* Animation */
      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(-10px);
        }

        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .form-title {
        margin: 0 0 1.5rem 0;
        color: var(--dark);
        font-size: 1.5rem;
        font-weight: 600;
        letter-spacing: -0.5px;
        text-align: center;
        position: relative;
        padding-bottom: 1rem;
        border-bottom: 2px solid var(--light-gray);
      }

      .form-title::after {
        content: "";
        position: absolute;
        bottom: -2px;
        left: 50%;
        transform: translateX(-50%);
        width: 60px;
        height: 3px;
        background: linear-gradient(90deg, var(--primary), var(--secondary));
        border-radius: 3px;
      }

      .form-group {
        margin-bottom: 1.5rem;
        display: flex;
        flex-direction: column;
      }

      .form-group label {
        margin-bottom: 0.5rem;
        font-weight: 600;
        color: var(--dark);
        font-size: 0.9rem;
      }

      .form-button {
        outline: none;
        border: none;
        padding: 5px;
        background-color: red;
        color: white;
        border-radius: 5px;
        font-weight: 600;
      }

      .empty-state {
        text-align: center;
        padding: 40px 20px;
        background: white;
        border-radius: var(--border-radius);
        box-shadow: var(--shadow);
      }

      .empty-state i {
        font-size: 3rem;
        color: var(--light-gray);
        margin-bottom: 15px;
      }

      .empty-state h3 {
        color: var(--gray);
        margin-bottom: 10px;
      }

      .empty-state p {
        color: var(--gray);
        opacity: 0.8;
      }

      @media (max-width: 1200px) {
        .bookings-container {
          grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
        }
      }

      @media (max-width: 992px) {
        .bodyContainer {
          grid-template-columns: 1fr;
        }

        .adCon {
          height: auto;
          position: relative;
          top: 0;
        }

        .ad-text {
          transform: rotate(0);
          padding: 30px;
        }

        .stats-container {
          grid-template-columns: repeat(2, 1fr);
        }
      }

      @media (max-width: 768px) {
        nav ul {
          gap: 10px !important;
          flex-wrap: wrap;
          justify-content: center;
        }

        .stats-container {
          grid-template-columns: 1fr;
        }

        .search-bar {
          flex-direction: column;
        }

        .booking-actions {
          flex-direction: column;
        }

        .booking-actions button {
          width: 100%;
        }
      }

      @media (max-width: 576px) {
        .bookings-container {
          grid-template-columns: 1fr;
        }

        .booking-card {
          padding: 15px;
        }
      }

      .management-section {
        background: white;
        border-radius: var(--border-radius);
        box-shadow: var(--shadow);
        padding: 25px;
        margin-bottom: 30px;
      }

      .management-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
      }

      .toggle-management {
        background-color: var(--primary);
        color: white;
        border: none;
        padding: 8px 15px;
        border-radius: 4px;
        cursor: pointer;
        transition: var(--transition);
      }

      .toggle-management:hover {
        background-color: #0b5ed7;
      }

      .space-list {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
        gap: 15px;
        margin-top: 20px;
      }

      .space-item {
        background: #f8f9fa;
        border-radius: 5px;
        padding: 15px;
        border-left: 4px solid var(--primary);
        position: relative;
      }

      .space-item h4 {
        margin-bottom: 10px;
        color: var(--dark);
      }

      .space-item p {
        margin-bottom: 5px;
        font-size: 0.9rem;
        color: var(--gray);
      }

      .space-actions {
        position: absolute;
        top: 10px;
        right: 10px;
        display: flex;
        gap: 5px;
      }

      .edit-space-btn,
      .delete-space-btn {
        background: none;
        border: none;
        cursor: pointer;
        font-size: 0.9rem;
        padding: 2px 5px;
        border-radius: 3px;
      }

      .edit-space-btn {
        color: var(--primary);
      }

      .delete-space-btn {
        color: var(--danger);
      }

      .space-form {
        display: none;
        background: white;
        border-radius: var(--border-radius);
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
        padding: 25px;
        width: 90%;
        max-width: 700px;
        max-height: 90vh;
        overflow-y: auto;
        position: fixed;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        z-index: 2100;
        animation: popupFadeIn 0.3s ease-out;
      }

      .space-form.active {
        display: block;
      }

      #formTitle {
        margin: 0 0 1.5rem 0;
        color: var(--dark);
        font-size: 1.5rem;
        font-weight: 600;
        letter-spacing: -0.5px;
        text-align: center;
        position: relative;
        padding-bottom: 1rem;
        border-bottom: 2px solid var(--light-gray);
      }

      #formTitle::after {
        content: "";
        position: absolute;
        bottom: -2px;
        left: 50%;
        transform: translateX(-50%);
        width: 60px;
        height: 3px;
        background: linear-gradient(90deg, var(--primary), var(--secondary));
        border-radius: 3px;
      }

      .form-row {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 15px;
      }

      .form-group {
        display: flex;
        flex-direction: column;
      }

      .form-group label {
        margin-bottom: 0.5rem;
        font-weight: 600;
        color: var(--dark);
        font-size: 0.9rem;
      }

      .form-group input,
      .form-group select,
      .form-group textarea {
        padding: 10px;
        border: 1px solid #ddd;
        border-radius: var(--border-radius);
        font-size: 0.95rem;
      }

      .form-actions {
        display: flex;
        justify-content: flex-end;
        gap: 10px;
        margin-top: 20px;
      }

      .form-actions button {
        padding: 10px 18px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-weight: 600;
        transition: var(--transition);
      }

      .form-actions .btn-primary {
        background-color: var(--primary);
        color: white;
      }

      .form-actions .btn-primary:hover {
        background-color: #0b5ed7;
      }

      .form-actions .btn-secondary {
        background-color: #6c757d;
        color: white;
      }

      .form-actions .btn-secondary:hover {
        background-color: #5a6268;
      }

      @media (max-width: 768px) {
        .form-row {
          grid-template-columns: 1fr;
        }
      }

      .rules-section {
        margin-top: 30px;
      }

      .rules-textarea {
        width: 100%;
        min-height: 150px;
        padding: 10px;
        border: 1px solid #ddd;
        border-radius: 5px;
        font-family: inherit;
      }

      .save-rules-btn {
        margin-top: 15px;
        background-color: var(--success);
        color: white;
        border: none;
        padding: 8px 15px;
        border-radius: 4px;
        cursor: pointer;
      }

      #closeSpaceForm {
        position: absolute;
        top: 10px;
        right: 15px;
        padding: 3px 5.5px 3px 5.5px;
        font-size: 22px;
        font-weight: bold;
        cursor: pointer;
        color: var(--text-light);
        transition: 0.3s ease-in-out;
      }

      #closeSpaceForm:hover {
        transform: rotatez(90deg);

        color: red;
      }

      /* Loading animation for auto-refresh */
      @keyframes pulse {
        0% {
          opacity: 1;
        }

        50% {
          opacity: 0.5;
        }

        100% {
          opacity: 1;
        }
      }

      .loading-indicator {
        animation: pulse 1s infinite;
      }

      /* Auto-refresh status indicator */
      .refresh-status {
        position: fixed;
        bottom: 20px;
        right: 20px;
        background: rgba(0, 0, 0, 0.8);
        color: white;
        padding: 8px 15px;
        border-radius: 20px;
        font-size: 0.8rem;
        z-index: 1000;
        display: none;
      }

      .refresh-status.show {
        display: block;
      }
    </style>
  </head>

  <body>
    <%- include('../../partials/managerNav.ejs') %>

    <div class="bodyContainer">
      <!-- AD SECTION -->
      <div class="adCon d-flex justify-content-center align-items-center">
        <div class="advertisement" id="ad-slider">
          <% if (locals.ads && ads.filter(ad=> ad.status==="Active").length > 0)
          { %> <% ads.filter(ad=> ad.status==="Active").forEach(ad => { %>
          <img
            src="http://localhost:3000/<%= ad.imagePath.replace(/\\/g, '/') %>"
            class="ad-slide"
            alt="Ad"
            height="100px"
            width="200px"
          />
          <% }) %> <% } else { %>
          <div
            class="d-flex justify-content-center align-items-center"
            style="
              background-color: rgba(158, 207, 250, 0.539);
              border-radius: 8px;
              color: rgb(0, 80, 193);
              border: 3px dotted blue;
              height: 100%;
              width: 100%;
            "
          >
            <p class="ad-text fs-4" style="font-weight: 600">Advertisement</p>
          </div>
          <% } %>
        </div>
      </div>

      <!-- CONTENT SECTION -->
      <div class="contentCon">
        <div class="section-title">
          <h1>Common Space Bookings</h1>
        </div>

        <!-- Management Section -->
        <div class="management-section">
          <div class="management-header">
            <h3 style="color: rgba(0, 0, 0, 0.669)">Manage Common Spaces</h3>
            <button class="toggle-management" id="toggleManagement">
              Show Management
            </button>
          </div>

          <div id="managementContent" style="display: none">
            <!-- Add New Space Form -->
            <div class="d-flex justify-content-end">
              <button class="toggle-management" id="addSpaceBtn">
                <i class="bi bi-plus"></i>Add New Space
              </button>
            </div>

            <div class="popup" id="spaceFormPopup" style="display: none">
              <div class="popup-content" id="spaceForm">
                <div class="d-flex justify-content-center align-items-start">
                  <h4 id="formTitle">Add New Common Space</h4>
                  <i class="bi bi-x rounded-circle" id="closeSpaceForm"></i>
                </div>
                <form id="spaceFormElement">
                  <div class="form-row">
                    <div class="form-group">
                      <label for="spaceType">Space Type</label>
                      <select
                        class="form-control"
                        id="spaceType"
                        name="spaceType"
                        required
                      >
                        <option value="">Select Type</option>
                        <option value="Clubhouse">Clubhouse</option>
                        <option value="Banquet Hall">Banquet Hall</option>
                        <option value="Swimming Pool">Swimming Pool</option>
                        <option value="Tennis Court">Tennis Court</option>
                        <option value="Badminton Court">Badminton Court</option>
                        <option value="Basketball Court">
                          Basketball Court
                        </option>
                        <option value="Indoor Games Room">
                          Indoor Games Room
                        </option>
                        <option value="Amphitheatre">Amphitheatre</option>
                        <option value="Guest Room">Guest Room</option>
                        <option value="Gym">Gym</option>

                        <option value="Other">Other</option>
                      </select>
                    </div>
                    <div class="form-group">
                      <label for="spaceName">Space Name</label>
                      <input
                        type="text"
                        class="form-control"
                        id="spaceName"
                        name="spaceName"
                        required
                      />
                    </div>
                  </div>

                  <div class="form-row">
                    <div class="form-group">
                      <label for="bookable">Bookable</label>
                      <select
                        class="form-control"
                        id="bookable"
                        name="bookable"
                      >
                        <option value="true">Yes</option>
                        <option value="false">No</option>
                      </select>
                    </div>
                    <div class="form-group">
                      <label for="maxHours">Max Booking Duration (hours)</label>
                      <input
                        type="number"
                        class="form-control"
                        id="maxHours"
                        name="maxHours"
                        min="1"
                        value="4"
                      />
                    </div>
                  </div>

                  <div class="form-group">
                    <label for="bookingRent">Rent (per hour)</label>
                    <input
                      type="text"
                      class="form-control"
                      id="bookingRent"
                      name="bookingRent"
                    />
                  </div>

                  <div class="form-group">
                    <label for="bookingRules">Booking Rules</label>
                    <textarea
                      class="form-control"
                      id="bookingRules"
                      name="bookingRules"
                      rows="3"
                    ></textarea>
                  </div>

                  <div class="form-actions">
                    <button
                      type="button"
                      class="btn btn-secondary"
                      id="cancelSpaceBtn"
                    >
                      Cancel
                    </button>
                    <button type="submit" class="btn btn-primary">
                      Save Space
                    </button>
                  </div>
                </form>
              </div>
            </div>

            <!-- Spaces List -->
            <div class="space-list" id="spacesList">
              <% if (locals.community && community.commonSpaces &&
              community.commonSpaces.length> 0) { %> <%
              community.commonSpaces.forEach(space=> { %>
              <div class="space-item" data-id="<%= space._id %>">
                <div class="space-actions">
                  <button
                    class="edit-space-btn"
                    data-id="<%= space._id %>"
                    data-name="<%= space.name %>"
                    data-type="<%= space.type %>"
                    data-bookable="<%= space.bookable %>"
                    data-maxhours="<%= space.maxBookingDurationHours %>"
                    data-rent="<%= space.rent %>"
                    data-rules="<%= space.bookingRules %>"
                  >
                    <i class="bi bi-pencil"></i>
                  </button>
                  <button class="delete-space-btn" data-id="<%= space._id %>">
                    <i class="bi bi-trash"></i>
                  </button>
                </div>
                <h4><%= space.name %></h4>
                <p>
                  <strong>Type:</strong>
                  <%= space.type %>
                </p>
                <p>
                  <strong>Bookable:</strong>
                  <%= space.bookable ? 'Yes' : 'No' %>
                </p>
                <% if (space.bookable) { %>
                <p>
                  <strong>Max Hours:</strong>
                  <%= space.maxBookingDurationHours || 'Not specified' %>
                </p>
                <% } %> <% if (space.bookingRules) { %>
                <p>
                  <strong>Rules:</strong>
                  <%= space.bookingRules.substring(0, 50) %>...
                </p>
                <% } %>
              </div>
              <% }) %> <% } else { %>
              <div class="empty-state">
                <i class="bi bi-building"></i>
                <h3>No Common Spaces Configured</h3>
                <p>Add your first common space to get started.</p>
              </div>
              <% } %>
            </div>

            <!-- Booking Rules Section -->
          </div>
        </div>

        <!-- Existing Booking Stats and List -->
        <div class="stats-container">
          <div class="stat-card" style="border-left: 6px solid var(--primary)">
            <h3>Today's Bookings</h3>
            <span class="stat-number" style="color: var(--primary)">
              <%= csb.length %>
            </span>
          </div>
          <div class="stat-card" style="border-left: 6px solid var(--danger)">
            <h3>Pending Approvals</h3>
            <span class="stat-number" style="color: var(--danger)">
              <%= csb.filter(c=> c.status === "Pending").length %>
            </span>
          </div>
          <div class="stat-card" style="border-left: 6px solid var(--success)">
            <h3>Approved</h3>
            <span class="stat-number" style="color: var(--success)">
              <%= csb.filter(c=> c.status === "Approved").length %>
            </span>
          </div>
        </div>

        <div class="search-bar">
          <input type="text" placeholder="Search by space, date or status..." />
          <button class="approve-all">Search</button>
          <button
            class="refresh-btn"
            id="manualRefresh"
            title="Refresh Bookings"
          >
            <i class="bi bi-arrow-clockwise"></i>
          </button>
        </div>

        <div class="bookings-container" id="bookingsContainer">
          <% if (locals.csb && csb.length> 0) { %> <% csb.forEach(c=> { %>
          <div class="booking-card">
            <div class="booking-card-header">
              <span class="booking-id">ID: <%= c.ID %></span>
              <span class="booking-status status-<%= c.status %>">
                <%= c.status %>
              </span>
            </div>

            <h3 class="booking-space"><%= c.name %></h3>

            <div class="booking-datetime">
              <i class="bi bi-calendar-event"></i>
              <span> <%= c.Date %>, <%= c.from %> - <%= c.to %> </span>
            </div>

            <div class="booking-actions">
              <% if (c.status==="Pending" ) { %>
              <button class="available-btn" data-id="<%= c._id %>">
                <i class="bi bi-eye"></i> Check Availability
              </button>
              <% } else if(c.status==="available" ){ %>
              <button class="approve-btn" data-id="<%= c._id %>">
                <i class="bi bi-check-circle-fill"></i> Approve
              </button>
              <button class="reject-btn" data-id="<%= c._id %>">
                <i class="bi bi-x-circle-fill"></i> Reject
              </button>
              <% } else { %>
              <button class="view-btn" data-id="<%= c._id %>">
                <i class="bi bi-eye"></i> View Details
              </button>
              <% } %>
            </div>
          </div>
          <% }) %> <% } else { %>
          <div class="empty-state w-100">
            <i class="bi bi-calendar-x"></i>
            <h3>No Bookings Found</h3>
            <p>There are currently no common space bookings to display.</p>
          </div>
          <% } %>
        </div>
      </div>
    </div>

    <div id="bookingDetailsPopup" class="popup" style="display: none">
      <div class="popup-content">
        <h3>Booking Details</h3>
        <button class="close-btn">x</button>

        <div id="detailsContent"></div>
      </div>
    </div>

    <div id="cancellationReasonPopup" class="popup" style="display: none">
      <div class="popup-content">
        <h3>Rejection Reason</h3>
        <textarea
          id="rejectionReason"
          rows="4"
          style="
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
          "
          placeholder="Enter reason for rejection..."
        ></textarea>
        <div style="margin-top: 15px; text-align: right">
          <button
            onclick="closeForm('cancellation')"
            style="
              background-color: #6c757d;
              color: white;
              border: none;
              padding: 8px 15px;
              border-radius: 4px;
              cursor: pointer;
              margin-right: 10px;
            "
          >
            Cancel
          </button>
          <button
            id="submitRejection"
            style="
              background-color: var(--danger);
              color: white;
              border: none;
              padding: 8px 15px;
              border-radius: 4px;
              cursor: pointer;
            "
          >
            Submit
          </button>
        </div>
      </div>
    </div>

    <script src="/notyf/notyf.min.js"></script>
    <script src="../../js/communityManager/CSB.js"></script>
    <script src="../../js/sidebar.js"></script>
  </body>
</html>
