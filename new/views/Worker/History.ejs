<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Work History Dashboard</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Outfit:wght@100..900&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="../../css/nav.css" />

    <style>
      

      .section-title {
        position: relative;
        padding-bottom: 15px;
        margin-bottom: 25px;
        border-bottom: 2px solid #e9ecef;
      }

      .section-title::after {
        content: "";
        position: absolute;
        bottom: -2px;
        left: 0;
        width: 250px;
        height: 3px;
        background: linear-gradient(90deg, var(--primary), var(--secondary));
        border-radius: 3px;
      }

      /* Work History Cards Container */
      .history-cards {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
        gap: 20px;
      }

      /* Work History Card */
      .history-card {
        background: white;
        border-radius: var(--border-radius);
        box-shadow: var(--shadow);
        padding: 20px;
        transition: var(--transition);
        border-left: 4px solid var(--primary);
        position: relative;
        overflow: hidden;
      }

      .history-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
      }

      .history-card-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 15px;
        padding-bottom: 10px;
        border-bottom: 1px solid var(--light-gray);
      }

      .task-id {
        font-size: 0.85rem;
        color: var(--gray);
        font-weight: 600;
      }

      .task-title {
        font-size: 1.2rem;
        font-weight: 600;
        color: var(--dark);
        margin-bottom: 10px;
        line-height: 1.3;
      }

      .status-review {
        background-color: #ecf1b0;
        color: #c6a900;
      }

      .task-details {
        margin-bottom: 15px;
      }

      .detail-row {
        display: flex;
        align-items: center;
        margin-bottom: 8px;
        font-size: 0.95rem;
      }

      .detail-row i {
        color: var(--primary);
        margin-right: 10px;
        width: 20px;
        text-align: center;
      }

      .task-location {
        color: var(--dark);
        font-weight: 500;
      }

      .task-date {
        color: var(--gray);
      }

      .task-status {
        position: absolute;
        top: 15px;
        right: 15px;
        font-size: 0.75rem;
        font-weight: 600;
        padding: 6px 12px;
        border-radius: 20px;
      }

      .status-completed {
        background-color: #d1e7dd;
        color: var(--success);
      }

      .status-pending {
        background-color: #0057ed3b;
        color: var(--primary);
      }

      .task-feedback {
        margin-top: 15px;
        padding-top: 15px;
        border-top: 1px dashed var(--light-gray);
      }

      .feedback-title {
        font-size: 0.9rem;
        color: var(--gray);
        margin-bottom: 8px;
        font-weight: 500;
      }

      .star-rating {
        display: flex;
        align-items: center;
      }

      .star-rating i {
        color: #ffc107;
        font-size: 1.1rem;
        margin-right: 3px;
      }

      .no-feedback {
        color: var(--gray);
        font-style: italic;
        font-size: 0.9rem;
      }

      /* Empty State */
      .empty-state {
        text-align: center;
        padding: 40px 20px;
        background: white;
        border-radius: var(--border-radius);
        box-shadow: var(--shadow);
        grid-column: 1 / -1;
      }

      .empty-state i {
        font-size: 3rem;
        color: var(--light-gray);
        margin-bottom: 15px;
      }

      .empty-state h3 {
        color: var(--gray);
        margin-bottom: 10px;
      }

      .empty-state p {
        color: var(--gray);
        opacity: 0.8;
      }

      /* Responsive Styles */
      @media (max-width: 1200px) {
        .history-cards {
          grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        }
      }

      @media (max-width: 992px) {
        .bodyContainer {
          grid-template-columns: 1fr;
        }

        .adCon {
          height: auto;
          position: relative;
          top: 0;
        }

        .ad-text {
          transform: rotate(0);
          padding: 30px;
        }
      }

      @media (max-width: 768px) {
        nav ul {
          gap: 10px !important;
          flex-wrap: wrap;
          justify-content: center;
        }

        .history-cards {
          grid-template-columns: 1fr;
        }
      }

      @media (max-width: 576px) {
        .history-card {
          padding: 15px;
        }

        .task-title {
          font-size: 1.1rem;
        }
      }

      /* Animation */
      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .history-card {
        opacity: 0;
        animation: fadeInUp 0.5s ease forwards;
      }

      .history-card:nth-child(1) { animation-delay: 0.1s; }
      .history-card:nth-child(2) { animation-delay: 0.2s; }
      .history-card:nth-child(3) { animation-delay: 0.3s; }
      .history-card:nth-child(4) { animation-delay: 0.4s; }
      .history-card:nth-child(5) { animation-delay: 0.5s; }
      .history-card:nth-child(6) { animation-delay: 0.6s; }
      .history-card:nth-child(7) { animation-delay: 0.7s; }
      .history-card:nth-child(8) { animation-delay: 0.8s; }
    </style>
  </head>

  <body>
    <%- include('../../partials/workerNav.ejs') %>

    <div class="bodyContainer">
      <!-- AD SECTION -->
      <div class="adCon d-flex justify-content-center align-items-center">
        <div class="advertisement" id="ad-slider">
          <% if (locals.ads && ads.filter(ad => ad.status==="Active").length > 0) { %> <% ads.filter(ad => ad.status==="Active").forEach(ad => { %>
          <img
            src="http://localhost:3000/<%= ad.imagePath.replace(/\\/g, '/') %>"
            class="ad-slide"
            alt="Ad"
            height="100px"
            width="200px"
          />
          <% }) %> <% } else { %>
          <div
            class="d-flex justify-content-center align-items-center"
            style="
              background-color: rgba(158, 207, 250, 0.539);
              border-radius: 8px;
              color: rgb(0, 80, 193);
              border: 3px dotted blue;
              height: 100%;
              width: 100%;
            "
          >
            <p class="ad-text fs-4" style="font-weight: 600">Advertisement</p>
          </div>
          <% } %>
        </div>
      </div>

      <!-- HISTORY SECTION -->
      <div class="contentCon">
        <div class="section-title">
          <h1>Work History</h1>
        </div>

        <div class="history-cards">
          <% if (locals.issues && issues.length > 0) { %> 
            <% issues.forEach(i => { %> 
              <% if (i.status !== "Pending" && i.status!=="Assigned") { %>
                <div class="history-card">
                  <div class="history-card-header">
                    <span class="task-id">Task #<%= i.issueID %></span>
                    <span class="task-status <%= i.status === "Payment Pending" ? "status-pending" : i.status === "Review Pending" ? "status-review" : "status-completed" %>">
                      <%= i.status %>
                    </span>
                  </div>

                  <h3 class="task-title"><%= i.title %></h3>

                  <div class="task-details">
                    <div class="detail-row">
                      <i class="bi bi-geo-alt"></i>
                      <span class="task-location">
                        <%= i.resident.uCode %>
                      </span>
                    </div>
                    <div class="detail-row">
                      <i class="bi bi-calendar-check"></i>
                      <span class="task-date">
                          Completed on <%= new Date(i.resolvedAt).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %>
                      </span>

                    </div>
                  </div>

                  <div class="task-feedback">
                    <div class="feedback-title">Feedback:</div>
                    <div class="ms-3">
                      <% if (i.feedback) { %>
                      <div class="star-rating">
                        <% for(let index = 0; index < i.rating; index++ ) { %>
                          <i class="bi bi-star-fill"></i>
                        <% } %>
                        <% for(let index = 0; index < (5 - i.rating); index++ ) { %>
                          <i class="bi bi-star"></i>
                        <% } %>
                        <span style="margin-left: 8px; font-size: 0.9rem;"><%= i.rating %>/5</span>
                      </div>
                    <% } else { %>
                      <div class="no-feedback">No feedback provided</div>
                    <% } %>
                    <span><%= i.feedback || " " %></span>
                    </div>
                  </div>
                </div>
              <% } %> 
            <% }) %> 
          <% } else { %>
            <div class="empty-state">
              <i class="bi bi-clipboard-x"></i>
              <h3>No Work History Found</h3>
              <p>Your completed tasks will appear here</p>
            </div>
          <% } %>
        </div>
      </div>
    </div>

    <script src="../../js/sidebar.js"></script>
  </body>
</html>