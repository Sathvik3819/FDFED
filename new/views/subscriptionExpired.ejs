<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Subscription Expired - Community Portal</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Outfit:wght@100..900&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="../../css/nav.css" />

    <style>
      .subscription-container {
        min-height: calc(100vh - 80px);
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, #f0f7ff 0%, #e8f2ff 100%);
        padding: 40px 20px;
      }

      .subscription-card {
        background: white;
        border-radius: var(--border-radius);
        box-shadow: var(--shadow);
        max-width: 600px;
        width: 100%;
        text-align: center;
        padding: 50px 40px;
        position: relative;
        overflow: hidden;
      }

      .subscription-card::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 6px;
        background: linear-gradient(90deg, var(--danger), var(--warning));
      }

      .expired-icon {
        width: 120px;
        height: 120px;
        background: linear-gradient(135deg, #fff5f5, #fee);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 30px;
        position: relative;
        animation: pulse 2s infinite;
      }

      .expired-icon i {
        font-size: 4rem;
        color: var(--danger);
      }

      @keyframes pulse {
        0% { box-shadow: 0 0 0 0 rgba(220, 53, 69, 0.4); }
        70% { box-shadow: 0 0 0 20px rgba(220, 53, 69, 0); }
        100% { box-shadow: 0 0 0 0 rgba(220, 53, 69, 0); }
      }

      .subscription-title {
        font-size: 2.2rem;
        font-weight: 700;
        color: var(--dark);
        margin-bottom: 20px;
        line-height: 1.2;
      }

      .subscription-subtitle {
        font-size: 1.3rem;
        color: var(--danger);
        font-weight: 600;
        margin-bottom: 30px;
      }

      .subscription-message {
        font-size: 1.1rem;
        color: var(--gray);
        line-height: 1.6;
        margin-bottom: 40px;
        max-width: 500px;
        margin-left: auto;
        margin-right: auto;
      }

      .contact-section {
        background: linear-gradient(135deg, #f8f9fa, #e9ecef);
        border-radius: 12px;
        padding: 30px;
        margin: 30px 0;
        border-left: 5px solid var(--primary);
      }

      .contact-title {
        font-size: 1.4rem;
        font-weight: 600;
        color: var(--primary);
        margin-bottom: 15px;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
      }

      .contact-info {
        font-size: 1rem;
        color: var(--dark);
        margin-bottom: 20px;
      }

      .contact-button {
        background: linear-gradient(135deg, var(--primary), var(--secondary));
        color: white;
        border: none;
        padding: 15px 30px;
        border-radius: 50px;
        font-weight: 600;
        font-size: 1.1rem;
        transition: var(--transition);
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 10px;
        cursor: pointer;
      }

      .contact-button:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 20px rgba(13, 110, 253, 0.3);
        color: white;
      }

      .feature-list {
        background: #f8f9fa;
        border-radius: 12px;
        padding: 25px;
        margin-top: 30px;
        text-align: left;
      }

      .feature-list h4 {
        color: var(--primary);
        font-weight: 600;
        margin-bottom: 20px;
        text-align: center;
      }

      .feature-item {
        display: flex;
        align-items: center;
        gap: 15px;
        padding: 10px 0;
        color: var(--gray);
      }

      .feature-item i {
        color: var(--success);
        font-size: 1.2rem;
        width: 20px;
      }

      .back-link {
        position: absolute;
        top: 20px;
        left: 20px;
        color: var(--gray);
        text-decoration: none;
        font-weight: 500;
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 8px 15px;
        border-radius: 8px;
        transition: var(--transition);
      }

      .back-link:hover {
        background: rgba(108, 117, 125, 0.1);
        color: var(--primary);
      }

      @media (max-width: 768px) {
        .subscription-card {
          padding: 40px 25px;
          margin: 20px;
        }

        .subscription-title {
          font-size: 1.8rem;
        }

        .subscription-subtitle {
          font-size: 1.1rem;
        }

        .subscription-message {
          font-size: 1rem;
        }

        .expired-icon {
          width: 100px;
          height: 100px;
        }

        .expired-icon i {
          font-size: 3rem;
        }

        .contact-section {
          padding: 20px;
        }

        .back-link {
          position: relative;
          top: auto;
          left: auto;
          margin-bottom: 20px;
          align-self: flex-start;
        }
      }

      .alert-badge {
        position: absolute;
        top: -10px;
        right: -10px;
        background: var(--danger);
        color: white;
        border-radius: 50%;
        width: 30px;
        height: 30px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.8rem;
        font-weight: bold;
        animation: bounce 1s infinite;
      }

      @keyframes bounce {
        0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
        40% { transform: translateY(-10px); }
        60% { transform: translateY(-5px); }
      }
    </style>
  </head>

  <body>
    <%- include('../../partials/managerNav.ejs') %>

    <div class="subscription-container">
      <div class="subscription-card">
        <a href="javascript:history.back()" class="back-link">
          <i class="bi bi-arrow-left"></i>
          Go Back
        </a>

        <div class="expired-icon">
          <i class="bi bi-shield-exclamation"></i>
          <div class="alert-badge">!</div>
        </div>

        <h1 class="subscription-title">Access Restricted</h1>
        <h2 class="subscription-subtitle">Subscription Expired</h2>
        
        <p class="subscription-message">
          Your community portal subscription has expired and access to the application has been temporarily suspended. 
          To continue enjoying all the features and services, please contact your community manager to renew your subscription.
        </p>

        <div class="contact-section">
          <div class="contact-title">
            <i class="bi bi-person-check"></i>
            Contact Community Manager
          </div>
          <p class="contact-info">
            Our community management team is ready to assist you with subscription renewal and answer any questions you may have.
          </p>
          <a href="mailto:manager@community.com" class="contact-button">
            <i class="bi bi-envelope"></i>
            Send Email
          </a>
        </div>

        <div class="feature-list">
          <h4>What You're Missing:</h4>
          <div class="feature-item">
            <i class="bi bi-check-circle-fill"></i>
            <span>Access to community dashboard and facilities</span>
          </div>
          <div class="feature-item">
            <i class="bi bi-check-circle-fill"></i>
            <span>Online payment and maintenance requests</span>
          </div>
          <div class="feature-item">
            <i class="bi bi-check-circle-fill"></i>
            <span>Event bookings and community announcements</span>
          </div>
          <div class="feature-item">
            <i class="bi bi-check-circle-fill"></i>
            <span>Visitor management and security features</span>
          </div>
        </div>

        <div class="mt-4">
          <small class="text-muted">
            <i class="bi bi-info-circle"></i>
            For immediate assistance, please contact your community manager or administrator.
          </small>
        </div>
      </div>
    </div>

    <script src="../../js/sidebar.js"></script>
    
    <script>
      // Add some interactivity
      document.addEventListener('DOMContentLoaded', function() {
        const contactButton = document.querySelector('.contact-button');
        
        contactButton.addEventListener('click', function(e) {
          // You can customize this to open a contact form modal instead
          // or redirect to a specific contact page
          console.log('Contact button clicked');
        });

        // Add fade-in animation
        const card = document.querySelector('.subscription-card');
        card.style.opacity = '0';
        card.style.transform = 'translateY(30px)';
        
        setTimeout(() => {
          card.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
          card.style.opacity = '1';
          card.style.transform = 'translateY(0)';
        }, 100);
      });
    </script>
  </body>
</html>